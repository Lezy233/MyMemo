# Tasks: stats-and-game

## 1. 数据库升级(v3 → v4)

- [ ] 1.1 `AppDatabaseHelper` 版本升至 4:users 表增加 quota_credit 默认 0,study_records 表增加 fail_count 默认 0,新增 word_daily_fails 表(user_id、word_id、fail_date、fail_count);从 v1 老包覆盖安装验证全链路升级、旧数据不丢失
- [ ] 1.2 实现数据访问方法:不认识次数 upsert(按用户+单词+当日)、学会时读取当日失败次数写入学习记录、额度余额查询与扣减;日志验证正确

## 2. word-learning 改造:失败次数持久化

- [ ] 2.1 背词判定逻辑改为:点"不认识"时写库(当日失败次数+1),学会时把当日失败次数写入 study_records;验证规格场景(点不认识计数入库、学会带失败次数、跨天重新计数);确认卡片色块仍为会话级行为不受影响

## 3. 统计界面(饼图)

- [ ] 3.1 实现四类统计查询:绿(今日学会 fail_count=0)、黄(fail_count=1)、红(当日失败≥2 去重,含最终学会的)、灰(剩余额度与未学会数公式);用构造数据验证分类边界(尤其"失败 2 次但最终学会"归红)
- [ ] 3.2 实现 `StudyPieChartView` 自定义 View:drawArc 按数量比例绘制四色扇形,旁配图例(色块+类别名+数量);统计界面嵌入进度环,onResume 重查重绘;验证无数据时提示而非空饼图

## 4. 贪吃蛇(SnakeGameActivity)

- [ ] 4.1 实现 `SnakeBoardView` 棋盘控件与游戏状态:21×21 网格,蛇起点 (11,11)、身长 1,果子随机空格生成;肉眼验证初始布局
- [ ] 4.2 实现游戏循环与操控:Handler 约 300ms 步进,四个方向按键控制,身长>1 时忽略反向输入,方向下一步生效;onPause 停表、onDestroy 移除回调;验证转向与禁止掉头场景
- [ ] 4.3 实现吃果子与胜负判定:吃果子身长+1、得分+1、新果子随机生成;撞墙/撞身失败,铺满 441 格胜利;结束弹结果对话框显示得分并可重开;验证三种结束场景
- [ ] 4.4 实现额度结算:对局结束一次性结算(果子数 + 胜利 100)写入 quota_credit,中途退出不结算;验证规格中失败结算、胜利结算、余额持久化三个场景

## 5. 额度兑换

- [ ] 5.1 主界面显示额度余额与兑换入口:对话框输入数量,校验正整数且不超过余额;兑换成功扣余额、加上限、立即生效并允许队列补充;验证规格中兑换成功、余额不足、非法数量三个场景

## 6. 集成验证

- [ ] 6.1 全流程走查:背词(制造绿/黄/红/灰四类数据)→ 统计界面饼图比例与数量正确 → 达到上限 → 玩贪吃蛇赢额度 → 兑换提升上限 → 继续背词 → 饼图与进度环同步更新;确认各新界面进度环显示一致
