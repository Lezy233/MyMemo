# Tasks: word-library

## 1. 数据库升级(v1 → v2)

- [ ] 1.1 `AppDatabaseHelper` 版本升至 2,`onCreate`/`onUpgrade` 中新增 words 表(word UNIQUE、meaning、is_preset)与 study_records 表(user_id、word_id、learned_at),users 表 `ALTER TABLE` 增加 daily_limit 默认 30;覆盖安装验证老数据(账号)不丢失、新表存在
- [ ] 1.2 实现预制词表播种:内置约 150 个单词(单词 + 释义),words 表为空时单事务批量插入;连续两次启动应用,验证单词数量不变(幂等)
- [ ] 1.3 实现词库与学习记录的数据访问方法:词库增删查改、按用户查今日已背数(按 learned_at 当日计数)、写入学习记录、查询未学会单词列表;用临时测试代码或日志验证

## 2. 词库管理界面(WordLibraryActivity)

- [ ] 2.1 实现单词列表界面:列表展示单词与释义,布局中嵌入进度环;真机或模拟器验证列表显示预制词表
- [ ] 2.2 实现添加单词:输入单词 + 释义提交,空值/重复单词分别提示,成功则入库并刷新列表;验证规格中三个场景
- [ ] 2.3 实现编辑与删除:修改释义保存后列表显示新值;删除确认后该词从列表消失;验证持久化(重启应用后仍生效)

## 3. 背词学习界面(StudyActivity)

- [ ] 3.1 实现卡片界面:显示英文单词,点击翻转显示释义与"认识/不认识"按钮;肉眼验证翻转交互
- [ ] 3.2 实现学习队列:会话开始查询未学会单词随机组建队列;"认识"streak+1、"不认识"清零并插回 3 张之后(不足则队尾);streak 达 2 写学习记录并出队;用日志打印队列变化验证判定逻辑(含"认识→不认识→认识→认识"才计学会的关键路径)
- [ ] 3.3 实现会话结束条件:队列空提示学习完成;已背数达上限终止并提示;验证两种结束场景

## 4. 每日上限与进度环接入

- [ ] 4.1 主界面增加"每日上限"设置入口:弹窗输入正整数,非法值拒绝,保存后立即生效;验证规格场景(30 改 50 后当日可继续学)
- [ ] 4.2 各 Activity `onResume` 查询真实已背数与上限并 `setProgress()`;验证规格场景(5 条记录显示 "5/30";背完返回主界面进度即时更新;无记录时显示 "0/30")

## 5. 集成验证

- [ ] 5.1 完整走查:登录 → 词库增删查改 → 背词(验证插回与连续两次规则)→ 达到上限被终止 → 修改上限后继续 → 进度环各处一致;次日(或改系统日期)验证已背数重新计数
