# Tasks: word-library

## 1. 数据库升级(v1 → v2)

- [X] 1.1 `AppDatabaseHelper` 版本升至 2,`onCreate`/`onUpgrade` 中新增 words 表(word UNIQUE、meaning、is_preset)与 study_records 表(user_id、word_id、learned_at),users 表 `ALTER TABLE` 增加 daily_limit 默认 30;覆盖安装验证老数据(账号)不丢失、新表存在
- [X] 1.2 实现预制词表播种:内置约 150 个单词(单词 + 释义),words 表为空时单事务批量插入;连续两次启动应用,验证单词数量不变(幂等)
- [X] 1.3 实现词库与学习记录的数据访问方法:词库增删查改、按用户查今日已背数(按 learned_at 当日计数)、写入学习记录、查询未学会单词列表;用临时测试代码或日志验证

## 2. 词库管理界面(WordLibraryActivity)

- [X] 2.1 实现单词列表界面:列表展示单词与释义,布局中嵌入进度环;真机或模拟器验证列表显示预制词表
- [X] 2.2 实现添加单词:输入单词 + 释义提交,空值/重复单词分别提示,成功则入库并刷新列表;验证规格中三个场景
- [X] 2.3 实现编辑与删除:修改释义保存后列表显示新值;删除确认后该词从列表消失;验证持久化(重启应用后仍生效)

## 3. 背词学习界面(StudyActivity)

- [X] 3.1 实现卡片界面:显示英文单词,点击翻转显示释义与"认识/不认识"按钮;肉眼验证翻转交互
- [X] 3.2 修订学习队列判定逻辑:会话开始查询未学会单词,随机打乱并按当日剩余额度(上限-已背)截取组建队列;初见单词点"认识"直接写库出队,点过"不认识"的单词清零并插回 3 张之后(不足则队尾)、此后连续两次"认识"才写库出队;用日志打印队列变化验证两条关键路径("初见认识直接计学会"、"不认识→认识→认识"后学会)
- [X] 3.3 实现会话结束条件:队列空提示学习完成;已背数达上限终止并提示;验证两种结束场景
- [X] 3.4 实现今日单词队列界面:分区显示今日已学会单词(查当日学习记录)与待背单词(读内存队列);验证队列容量不超过剩余额度、上限提升后队列补充新词
- [X] 3.5 实现卡片点击记录色块:释义下方按点击顺序追加蓝色("认识")/红色("不认识")方格,插回后再次出现时保留本次会话色块;退出背词界面重进验证色块按会话清空

## 4. 每日上限与进度环接入

- [X] 4.1 主界面增加"每日上限"设置入口:弹窗输入正整数,非法值拒绝,保存后立即生效;验证规格场景(30 改 50 后当日可继续学)
- [X] 4.2 各 Activity `onResume` 查询真实已背数与上限并 `setProgress()`;验证规格场景(5 条记录显示 "5/30";背完返回主界面进度即时更新;无记录时显示 "0/30")

## 5. 集成验证

- [X] 5.1 完整走查:登录 → 词库增删查改 → 背词(验证初见认识即学会、插回与连续两次规则、色块显示)→ 队列界面查看已学会/待背 → 达到上限被终止 → 修改上限后继续且队列补充 → 进度环各处一致;次日(或改系统日期)验证已背数重新计数
