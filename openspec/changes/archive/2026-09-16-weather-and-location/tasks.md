# Tasks: weather-and-location

## 1. 数据库升级(v4 → v5)

- [x] 1.1 `AppDatabaseHelper` 版本升至 5,`onCreate`/`onUpgrade` 新增 weather_queries 表(latitude、longitude、queried_at、result JSON 原文);覆盖安装验证旧数据不丢失
- [x] 1.2 实现天气查询记录的数据访问方法:插入查询记录、查最近一次记录、按时间倒序查全部;日志验证存取正确

## 2. 定位

- [x] 2.1 Manifest 声明 `ACCESS_FINE_LOCATION` 与 `INTERNET` 权限;实现运行时定位权限申请;验证授权与拒绝两条路径
- [x] 2.2 实现定位获取(LocationManager,约 10 秒超时)与默认坐标兜底:权限被拒、超时、无信号三种情况均回落到 (121.44, 31.19) 并提示"当前使用默认位置";真机或模拟器验证三个场景

## 3. 天气 API 适配层

- [x] 3.1 定义 `WeatherProvider` 接口(输入经纬度,输出今日 + 7 天结构化预报)与天气状况码中文映射表(含"未知"兜底)
- [x] 3.2 实现 `OpenMeteoProvider`:子线程 HttpURLConnection 请求、org.json 解析;**先在目标演示设备/模拟器上验证连通性**,不可达则改实现 `AmapProvider`(高德天气,需注册 key)并记录选型结果;验证返回今日 + 7 天数据
- [x] 3.3 查询成功后将坐标、时间、预报 JSON 原文写入 weather_queries 表;日志验证入库

## 4. 天气界面(WeatherActivity)

- [x] 4.1 实现天气界面:顶部当前坐标(或默认位置提示)+ 刷新按钮、今日天气卡片、未来 7 天预报 ListView;布局嵌入进度环;进入界面自动执行"定位 → 查询"
- [x] 4.2 实现失败处理:请求失败且有缓存时展示最近一次结果并标注查询时间;无缓存时提示检查网络;飞行模式下分别验证两条路径

## 5. 查询历史界面(WeatherHistoryActivity)

- [x] 5.1 实现历史列表:ListView 按时间倒序展示查询时间、坐标与当日天气摘要;布局嵌入进度环;无记录时提示;主界面增加天气入口

## 6. 集成验证

- [x] 6.1 全流程走查:首次进入(权限申请)→ 定位成功/兜底 → 查询成功并展示今日 + 7 天 → 断网刷新展示缓存 → 查询历史可见多条记录 → 切换账号后缓存仍可用;确认新界面进度环显示一致;在最终演示设备上完整彩排一次
