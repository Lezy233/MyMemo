# Tasks: friends-and-chat

## 1. 数据库升级(v2 → v3)

- [x] 1.1 `AppDatabaseHelper` 版本升至 3,`onCreate`/`onUpgrade` 新增 friendships 表(user_id、friend_id、status: pending/accepted、created_at)与 messages 表(sender_id、receiver_id、content、sent_at);覆盖安装验证旧数据(账号、词库、学习记录)不丢失
- [x] 1.2 实现好友关系数据访问方法:发起申请(含自己/重复/已是好友校验)、查待处理申请、同意(status 改 accepted)、拒绝(删除记录)、双向匹配查询好友列表、删除好友;用两个测试账号交叉验证
- [x] 1.3 实现消息数据访问方法:发送消息(空内容拒绝)、按双方 id 查全部消息并按 sent_at 排序;日志验证存取正确

## 2. 好友申请

- [x] 2.1 实现添加好友入口:好友列表界面提供搜索用户名发起申请的对话框;验证规格场景(成功、添加自己、重复申请、用户不存在)
- [x] 2.2 实现申请列表界面(FriendRequestActivity):展示待处理申请,同意建立双向好友关系、拒绝删除记录;空列表有提示;布局嵌入进度环

## 3. 好友列表(FriendListActivity)

- [x] 3.1 实现好友列表:ListView + BaseAdapter,每项显示头像、用户名、今日已背数(复用按用户统计当日学习记录的查询);布局嵌入进度环;onResume 刷新验证背词数更新场景
- [x] 3.2 实现删除好友:长按好友项弹确认对话框,确认后双向解除关系且聊天记录保留;验证删除后任一方可重新申请
- [x] 3.3 主界面增加好友入口,申请入口显示待处理申请数量;点击好友项跳转聊天界面并传入好友信息

## 4. 聊天界面(ChatActivity)

- [x] 4.1 实现聊天气泡布局:ListView + 双布局适配器,本人消息右侧气泡、对方消息左侧气泡;按时间排序显示历史消息;无消息时提示;布局嵌入进度环
- [x] 4.2 实现发送消息:底部输入框 + 发送按钮,空内容不发送,发送成功写入数据库、刷新列表并滚动到底部;验证规格场景

## 5. 集成验证

- [x] 5.1 双账号全流程走查:注册 A、B 两账号 → A 搜索 B 发起申请(含各失败场景)→ 退出登录换 B → 同意申请 → 双方好友列表互见且今日已背数正确 → A、B 互发消息 → 验证左右气泡与持久化 → 删除好友后聊天记录保留、可重新申请;全程各界面进度环显示一致
